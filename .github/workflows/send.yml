name: Send email hourly
on:
  schedule:
    - cron: '0 9-18 * * 1-5'
  workflow_dispatch:
jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - run: python bx24.py
